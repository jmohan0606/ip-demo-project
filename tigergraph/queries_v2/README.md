# V2 Query Backup

Primary supported queries for this rebuild are TigerGraph 4.2.2 compatible V1/WebShell-friendly GSQL files in `queries_v1`.

This folder is reserved for future V2 syntax variants after the V1 package is validated in your AWS TigerGraph environment.
