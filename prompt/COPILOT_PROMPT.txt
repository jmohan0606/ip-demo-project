GOAL: Make the ENTIRE application genuinely TigerGraph-backed. In
GRAPH_CLIENT_MODE=real, ALL data must come from TigerGraph via real queries.
The in-memory mock/foundation store must be used ONLY as a genuine fallback
when TigerGraph actually fails — never as the default serving path in real mode,
and never as a silent substitute. TigerGraph 4.2.2 auth is working (health
healthy:true, tier 2 serving, 189/192 loaded). Build on that.

WORK CONTINUITY (important — this is a large migration):
- Track everything in a dedicated file STORE_MIGRATION.md: a checklist of every
  file to migrate, its status (DONE / IN-PROGRESS / REMAINING / BLOCKED-with-
  reason), what was changed, and the verification result per file.
- Update STORE_MIGRATION.md continuously as you go, and PROGRESS.md at
  boundaries. Commit + push after each file or small group is migrated and
  verified, so nothing is lost.
- If you approach a session/usage limit before finishing, STOP at a clean,
  committed checkpoint, write exactly what's DONE vs REMAINING and the precise
  next file to resume from in STORE_MIGRATION.md, and tell me clearly to
  continue. Do NOT rush or fake completion to appear done — an honest resumable
  checkpoint is required. I will resume you from STORE_MIGRATION.md until every
  file is done.

=== PART A — FIX THE 3 FAILED EDGE TYPES (close 192/192) ===
Run-All completed 189/192; 3 edge types FAILED (live TigerGraph rejected them —
schema/install mismatch on the remote graph):
- phx_dm_coaching_task_for_advisor
- phx_dm_coaching_task_assigned_by
- phx_dm_transition_of_recommendation
1. Investigate: are they installed on the live graph (query getSchema) vs.
   present in source-of-truth schema files
   (docs/tigergraph_foundation/tigergraph/schema/)? Compare these 3 edge
   definitions against edges that loaded successfully for a definition
   difference (FROM/TO types, reverse-edge naming, attribute types) the live
   4.2.2 engine rejects.
2. Fix root cause: if not installed, generate correct GSQL and install on live
   TigerGraph (or output exact GSQL for me to run — tell me which); if malformed
   for 4.2.2, correct in schema files AND install.
3. Re-run full "Run All"; confirm 192/192, 0 failed, the 3 edges now loading
   rows. Show the real result.

=== PART B — MIGRATE EVERY DIRECT .store READER TO REAL TigerGraph ===
Migrate ALL of the following (complete audited list). The hard rule for every
one: in GRAPH_CLIENT_MODE=real, data comes from TigerGraph via run_query (or a
typed repository/GraphClient method dispatching to tier 2); the store is touched
ONLY if the real TigerGraph query genuinely fails (real fallback), and such a
fallback must be LOGGED, never silent.

HIGH-IMPACT (demo-facing) — do FIRST:
- app/scope/dashboard.py
- app/scope/rollup.py
- app/revenue/analytics.py
- app/revenue/trend_explorer.py
- app/api/routers/advisor360.py
- app/client360/service.py
- app/api/routers/hierarchy.py
- app/peers/benchmarking.py
- app/services/pipeline_trace_service.py

REMAINING — migrate ALL of these too:
- app/agp/mentorship.py
- app/agp/service.py
- app/api/routers/search_notifications.py
- app/coaching/service.py
- app/crm/service.py
- app/embeddings/similar_entities.py
- app/graph/neighborhood.py
- app/graph/memory/memory_seeder.py
- app/recommendations/lifecycle.py
- app/recommendations/service.py
- app/repositories/state_repository.py

(Leave mock-tier internals in app/graph/client.py and app/graph/tiered_client.py
as-is — those ARE the mock store implementation.)

For each file:
1. Identify every direct .store read and the data it fetches.
2. Replace with the real TigerGraph query via run_query, using installed GQ-###
   queries where they exist (get_advisor_360, get_org_hierarchy,
   get_scope_descendants, get_household_360, get_account_holdings, etc.).
3. If a needed read has NO matching query: create the appropriate GSQL query
   (V1 syntax, matching the existing query conventions) and install it, rather
   than falling back to store. Only if a real query genuinely cannot be created/
   run, mark that specific read BLOCKED in STORE_MIGRATION.md with the reason —
   do not silently leave it on store.
   NEW QUERY FILE REQUIREMENT — whenever you create a new GSQL query (because a
	.store read has no matching existing query):
	- Save it as a proper query file in the source-of-truth queries folder
	  (docs/tigergraph_foundation/tigergraph/queries/), following the EXISTING
	  naming + format convention: next sequential GQ-### number, header comment
	  block (name, purpose, outputs) matching the other query files, SYNTAX V1,
	  correct V1 parameter order (TYPE name) and V1 traversal patterns (vertex-type
	  targets, edge aliases, one hop per SELECT) — consistent with the 50 existing
	  queries.
	- Add it to the query catalog/manifest (query_catalog.json and any
	  install_all_queries.gsql / test cases) so it's part of the installable set and
	  reproduces on a fresh rebuild.
	- Install it on the live TigerGraph AND confirm it installs cleanly (same V1
	  rules that were needed for the existing queries).
	- List every newly-created query (file name + purpose) in STORE_MIGRATION.md and
	  the final report.
	Do NOT create ad-hoc queries that only exist on the live graph — every new query
	must be a committed file in the source-of-truth queries folder so it survives a
	rebuild and lives in the base repo.
4. Preserve behavior EXACTLY — same data shape to the UI; only change the SOURCE
   (TigerGraph, not in-memory store).
5. Fallback discipline: store fallback ONLY on real TigerGraph failure, and it
   MUST be logged (so we can see when/why real failed). Never default to store
   in real mode; never silently substitute mock for a working real path.
6. app/repositories/state_repository.py already has a TigerGraph+SQLite adapter
   (StateRepository) — make its reads TigerGraph-authoritative in real mode,
   consistent with that pattern.

VERIFY per file (real evidence, not assumption):
- With GRAPH_CLIENT_MODE=real and data loaded, exercise the migrated screen/
  endpoint; confirm it serves from TIER 2 (served_by_tier / tier log = tier 2
  pyTigerGraph, not tier 4 mock), rendering real data.
- Spot-check figures match TigerGraph (e.g. dashboard advisor count = 60).
- Backend boots, frontend renders, no regressions.
- Record the per-file verification in STORE_MIGRATION.md.

=== FINAL VERIFICATION (when all done) ===
- Grep the codebase: no application service (outside the mock-tier internals)
  reads .store as its PRIMARY path in real mode. Every .store touch is either
  the mock tier itself or a logged real-failure fallback.
- End to end in real mode: dashboard, revenue, revenue-trend, advisor360,
  client360, hierarchy, peer-benchmarking, scope/rollup, AGP, coaching, CRM,
  recommendations, pipeline-trace all serve from TigerGraph (tier 2), verified.
- 192/192 entities loaded in TigerGraph.

=== REPORT ===
- Part A: root cause + fix + 192/192 proof.
- Part B: STORE_MIGRATION.md with every file's status + per-file tier-2
  verification; any BLOCKED items with precise reasons (no silent store
  defaults).
- Update PROGRESS.md.
NEW QUERY FILE REQUIREMENT — whenever you create a new GSQL query (because a
.store read has no matching existing query):
- Save it as a proper query file in the source-of-truth queries folder
  (docs/tigergraph_foundation/tigergraph/queries/), following the EXISTING
  naming + format convention: next sequential GQ-### number, header comment
  block (name, purpose, outputs) matching the other query files, SYNTAX V1,
  correct V1 parameter order (TYPE name) and V1 traversal patterns (vertex-type
  targets, edge aliases, one hop per SELECT) — consistent with the 50 existing
  queries.
- Add it to the query catalog/manifest (query_catalog.json and any
  install_all_queries.gsql / test cases) so it's part of the installable set and
  reproduces on a fresh rebuild.
- Install it on the live TigerGraph AND confirm it installs cleanly (same V1
  rules that were needed for the existing queries).
- List every newly-created query (file name + purpose) in STORE_MIGRATION.md and
  the final report.
Do NOT create ad-hoc queries that only exist on the live graph — every new query
must be a committed file in the source-of-truth queries folder so it survives a
rebuild and lives in the base repo.

HARD REQUIREMENTS: TigerGraph is the source of truth for ALL reads in real mode.
Mock/store is ONLY a genuine, logged fallback for real failures. No faked
success. If bigger than one session, checkpoint honestly in STORE_MIGRATION.md
and tell me to continue — never leave something half-migrated-but-marked-done.